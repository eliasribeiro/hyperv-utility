<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="HyperVUtilities.Pages.CreateVmGpuPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HyperVUtilities.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Margin="40">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="20">
            <FontIcon Glyph="&#xE7F4;" FontSize="64" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
            <TextBlock Text="Criar VM com GPU" 
                      FontSize="24" 
                      FontWeight="SemiBold" 
                      HorizontalAlignment="Center"/>
            <TextBlock Text="Esta funcionalidade será implementada em breve." 
                      FontSize="16" 
                      Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                      HorizontalAlignment="Center"/>
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="20"
                    MaxWidth="600">
                <TextBlock Text="Em desenvolvimento: Interface para criação de máquinas virtuais com suporte a GPU passthrough para melhor performance em aplicações gráficas e computação paralela."
                          TextWrapping="Wrap"
                          FontSize="14"
                          HorizontalAlignment="Center"/>
            </Border>
        </StackPanel>
    </Grid>
</Page> 